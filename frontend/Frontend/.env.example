# Frontend environment variables
# Copy this file to .env and adjust values

# API base URL (leave empty to use Vite proxy / relative URLs in production via nginx).
# Tokens are delivered via HttpOnly cookies, so no token is read by JS.
VITE_API_URL=
